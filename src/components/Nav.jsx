
const Nav = () => {
  return (
    <div>
      <div className="navbar justify-center">
        <a onClick={() => window.location.reload()} className="btn btn-ghost normal-case text-2xl font-bold text-[#1C1B1B]">Course Registration</a>
      </div>
    </div>
  );
};

export default Nav;